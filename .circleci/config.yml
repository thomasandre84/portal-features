version: 2.1

jobs:
  build:
    docker:
      - image: 'cimg/openjdk:15.0.0-node'
    steps:
      - checkout
      - run:
          name: Analyze on SonarCloud
          command: mvn install sonar:sonar

workflows:
  main:
    jobs:
      - build:
          context: SonarCloud
